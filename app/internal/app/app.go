package app

import (
	// go lib
	"context"

	// Others lib.
	""

	// lib from our projects.
)

// App for initialize chi router, grpc and policy NewConstructor(but for strcut)
type App struct {
	//I will be write it later. and show you now i prefer write code up to down.
	// From Net layer to storage.
}